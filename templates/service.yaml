apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-template-nestjs.fullname" . }}
  labels:
    {{- include "app-template-nestjs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "app-template-nestjs.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
